@using StandardEng.Web.Reports

@{
    var typeReportSource = new TypeReportSource { TypeName = typeof(PartsPI).AssemblyQualifiedName };
    typeReportSource.Parameters.Add("PerformaInvoiceId", ViewBag.PerformaInvoiceId);
    typeReportSource.Parameters.Add("ReportName", ViewBag.Reportname);
}
<style>
    #RpPartsQuotation {
        position: absolute;
        left: 5px;
        right: 5px;
        top: 5px;
        bottom: 5px;
        overflow: hidden;
    }
</style>

@(Html.TelerikReporting().ReportViewer()
                   .Id("RpPartsQuotation")
                   .ServiceUrl(WebHelper.ReportApiController)
                   .TemplateUrl(WebHelper.ReportTemplate)
                   .ReportSource(typeReportSource)
                   .ViewMode(ViewMode.PrintPreview)
                   .ScaleMode(ScaleMode.Specific)
                   .Scale(1.0)
                   .PersistSession(false)
)



